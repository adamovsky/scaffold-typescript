export type { FunctionComponent, MouseEvent } from 'react';

export type Props = {
    className?: string,
    errorCode: number,
    testId?: string,
    text: string
};

className = '', errorCode, testId = TEST_ID, text = '' 